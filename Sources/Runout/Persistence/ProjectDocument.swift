import Foundation

// ReferenceFileDocument conformance for the .runout package format (manifest.json + master
// recordings + peak caches + artwork). See docs/DATA_MODEL.md (Project package layout).
// Implemented in M7 — see docs/ROADMAP.md.

