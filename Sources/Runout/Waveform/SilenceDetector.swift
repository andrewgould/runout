import Foundation

// Proposes track-break markers from gaps below a configurable dBFS threshold. Proposals are
// reviewed by the user, never auto-committed — see docs/FEATURES.md §2.
// Implemented in M8 (stretch) — see docs/ROADMAP.md.

