import Foundation

// Generates the multi-resolution min/max peak cache (.peaks format) from a finished recording.
// See docs/DATA_MODEL.md (Peak cache format). Implemented in M3 — see docs/ROADMAP.md.

