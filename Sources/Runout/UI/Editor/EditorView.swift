import SwiftUI

/// Screen 2 — see docs/UI_SPEC.md and assets/mockups/02-waveform-editor.png. Implemented in M3/M4.
struct EditorView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Split Into Tracks",
            systemImage: "waveform",
            message: "Waveform display and marker editing land in M3/M4."
        )
    }
}
