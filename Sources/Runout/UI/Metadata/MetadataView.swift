import SwiftUI

/// Screen 3 — see docs/UI_SPEC.md and assets/mockups/03-metadata.png. Implemented in M5.
struct MetadataView: View {
    var body: some View {
        PlaceholderScreen(
            title: "Track & Album Metadata",
            systemImage: "tag",
            message: "Per-track and album tagging lands in M5."
        )
    }
}
